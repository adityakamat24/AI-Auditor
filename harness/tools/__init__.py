"""Harness tools (PRD §9.7.4) — the signed catalog of callable tools. STUB — Phase 2/4.

Tools the agents can invoke, grouped into ``atomicwork`` (demo ITSM) and ``generic`` (http/file/shell). The
registry holds the signed catalog (name -> schema + signature) the auditor checks against (ASI04).
"""

from __future__ import annotations

# TODO(phase2): expose the signed tool catalog (atomicwork + generic) via registry.

__all__: list[str] = []
